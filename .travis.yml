language: python
python:
  - "3.6"
install:
  - pip install tox coveralls
services:
  - postgresql
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
script:
  - tox
after_success:
  - coveralls
